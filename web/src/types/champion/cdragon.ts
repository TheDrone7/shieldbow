/**
 * Community Dragon Champion Structure (left at `any` because it is quite messy).
 */
export interface ICDragonChampion {
  // TODO: Clean this up, with only required properties.
  [key: string]: any;
}
