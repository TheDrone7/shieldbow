/**
 * A representation of a game type (e.g. Custom Game)
 */
export interface GameType {
  /**
   * The ID of the game type.
   */
  gametype: string;
  /**
   * A text description of the game type.
   */
  description: string;
}
