/**
 * A representation of a game mode (e.g. Classic)
 */
export interface GameMode {
  /**
   * The ID of the game mode.
   */
  gameMode: string;
  /**
   * A text description of the game mode.
   */
  description: string;
}
