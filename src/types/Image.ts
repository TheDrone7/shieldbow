/**
 * A representation of any image data returned by Data Dragon.
 */
export interface ImageData {
  full: string;
  sprite: string;
  group: string;
  x: number;
  y: number;
  w: number;
  h: number;
}
